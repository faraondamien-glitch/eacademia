import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Couleurs primaires Granions
  static const Color primary = Color(0xFF003B7A);
  static const Color primaryLight = Color(0xFF1A5599);
  static const Color primaryDark = Color(0xFF002855);

  // Couleur secondaire (or)
  static const Color secondary = Color(0xFFB07D2C);
  static const Color secondaryLight = Color(0xFFCB9E4A);
  static const Color secondaryDark = Color(0xFF8A6020);

  // Succès
  static const Color success = Color(0xFF1D9E75);
  static const Color successLight = Color(0xFF25C48F);
  static const Color successDark = Color(0xFF157A5A);

  // États
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Fonds
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFEEF1F6);

  // Dark mode
  static const Color backgroundDark = Color(0xFF0F1923);
  static const Color surfaceDark = Color(0xFF1A2635);
  static const Color surfaceVariantDark = Color(0xFF243447);

  // Texte
  static const Color textPrimary = Color(0xFF1A2635);
  static const Color textSecondary = Color(0xFF6B7A8D);
  static const Color textDisabled = Color(0xFFB0BAC9);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Bordures
  static const Color border = Color(0xFFDDE3EC);
  static const Color divider = Color(0xFFEEF1F6);

  // Badges de rôle
  static const Color pharmacienBadge = Color(0xFF003B7A);
  static const Color medecinBadge = Color(0xFF1D9E75);
  static const Color kineBadge = Color(0xFFB07D2C);
  static const Color commercialBadge = Color(0xFF7C3AED);
}
